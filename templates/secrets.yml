---

production:
  db_key_base: '{{ gitlab_db_key_base }}'
  secret_key_base: '{{ gitlab_secret_key_base }}'
  otp_key_base: '{{ gitlab_otp_key_base }}'
  jws_private_key: '{{ gitlab_jws_private_key }}'

development:
  db_key_base: development

test:
  db_key_base: test
